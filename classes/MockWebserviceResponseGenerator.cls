@isTest
global class MockWebserviceResponseGenerator implements WebServiceMock {
   global void doInvoke(
           Object stub,
           Object request,
           Map<String, Object> response,
           String endpoint,
           String soapAction,
           String requestName,
           String responseNS,
           String responseName,
           String responseType) {/*

        // Create response element from the autogenerated class.
        TPO_PVsimIntegration.RunSimulationResponse_element resp = new TPO_PVsimIntegration.RunSimulationResponse_element();
        
        TPO_PVsimIntegration.SimulationResponse simresp = new TPO_PVsimIntegration.SimulationResponse();
        simresp.Status = 'Ok';
         // Populate response element.
        resp.RunSimulationResult = simresp;
        // Add response element to the response parameter, as follows:
        response.put('response_x', resp); 
   }
   /*
    global void doInvoke(
           Object stub,
           Object request,
           Map<String, Object> response,
           String endpoint,
           String soapAction,
           String requestName,
           String responseNS,
           String responseName,
           String responseType) {
           
       TPO_PVsimIntegration.SimulationResponse   resp = new TPO_PVsimIntegration.SimulationResponse();
             resp.Status ='it is ok';
       TPO_PVsimIntegration.RunSimulationResponse_element fresult = new TPO_PVsimIntegration.RunSimulationResponse_element();
       fresult.RunSimulationResult = resp;
      
       response.put('response_x', fresult);
        
   */}
}